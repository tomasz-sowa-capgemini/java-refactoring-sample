package com.gildedrose;

import io.cucumber.junit.Cucumber;
import io.cucumber.junit.CucumberOptions;
import org.junit.runner.RunWith;

/**
 * Test runner class for executing Cucumber feature files.
 * 
 * <p>This class uses JUnit to run Cucumber tests defined in the project.
 * It serves as the entry point for behavior-driven development (BDD) tests.</p>
 */
@RunWith(Cucumber.class)
@CucumberOptions
public class RunCucumberTest {
    // This class intentionally left empty.
    // Cucumber will automatically detect and run feature files based on configuration.
}